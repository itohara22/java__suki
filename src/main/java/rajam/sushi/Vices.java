package rajam.sushi;

public interface Vices {
     void getAttention();
    // public void getAttention(); this is treated same as above as in interfaces
    // all methods are public and abstract by default
}
